import React, { Component } from 'react';
import {View} from 'react-native';

export default class VerticalMargin extends Component {
    render() {
      return (
        <View style={{marginTop: 10}}></View>
      );
    }
  }